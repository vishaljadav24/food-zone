import { ScaledSheet } from "react-native-size-matters";

/* The code is exporting a default object created using the `ScaledSheet.create()` function from the
`react-native-size-matters` library. */
export default ScaledSheet.create({
  footer: {
    flexDirection: "row",
    alignItems: "center",
  },
  detailsContainer: {
    flex: 1,
    flexDirection: "row",
  },
});
