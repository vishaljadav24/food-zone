/* The code is exporting a constant object named `AppContants` with three properties: `DeliveryFee`,
`PlatFormFee`, and `GSTAndRestaurantCharges`. Each property has a corresponding value assigned to
it. */
export const AppContants = {
  DeliveryFee: 15,
  PlatFormFee: 5,
  GSTAndRestaurantCharges: 100,
};
