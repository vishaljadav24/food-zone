/* The code is exporting an object with three properties: `EmailRequired`, `PasswordRequired`, and
`EmailInvalid`. Each property is a string that represents an error message. This code is likely used
for handling form validation in a TypeScript project. */
export default {
  EmailRequired: "Email is required",
  PasswordRequired: "Password is required",
  EmailInvalid: "Email is invalid",
};
