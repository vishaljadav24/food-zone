import { ScaledSheet } from "react-native-size-matters";

/* The code `export default ScaledSheet.create({ loginBtn: { alignSelf: "center", }, });` is exporting
a JavaScript object that defines a style for a login button. */
export default ScaledSheet.create({
  loginBtn: {
    alignSelf: "center",
  },
});
